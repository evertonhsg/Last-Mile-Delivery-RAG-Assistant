# ── Core ──────────────────────────────────────────────────────────────────────
langchain==0.3.14
langchain-community==0.3.14

# ── Embeddings ────────────────────────────────────────────────────────────────
sentence-transformers==3.3.1
torch==2.5.1                   # CPU-only; for GPU: torch==2.5.1+cu121
langchain-huggingface==0.1.2

# ── Vector stores ─────────────────────────────────────────────────────────────
faiss-cpu==1.9.0
chromadb==0.5.23                # pinned <0.6.0: required by langchain-chroma==0.2.0
langchain-chroma==0.2.0

# ── Notebooks ─────────────────────────────────────────────────────────────────
jupyter==1.1.1
ipykernel==6.29.5
ipywidgets==8.1.5

# ── Data & utilities ──────────────────────────────────────────────────────────
numpy==1.26.4
scikit-learn==1.6.0
python-dotenv==1.0.1
pypdf==5.1.0

# ── Generation (Phase 2) ────────────────────────────────────────────────────
langchain-ollama==0.2.3

# ── Fine-tuning (Phase 3) ─────────────────────────────────────────────────────
# Apple Silicon (M-series) only — built on the MLX array framework, which
# targets Apple's unified memory architecture directly. Will not install on
# Intel Macs or non-Apple hardware.
#
# Note: pulls in transformers==5.17.0 and tokenizers==0.23.2, which pip flags
# as conflicting on paper with chromadb's (tokenizers<=0.20.3) and
# sentence-transformers' (transformers<5.0.0) pins above. Verified working
# end-to-end anyway as of 2026-09-20 — embeddings, cross-encoder, Chroma
# retrieval, and mlx-lm generation all tested together in the same venv with
# no runtime errors. No environment changes made; documenting this so a
# future "why does pip complain" moment isn't a surprise.
mlx-lm==0.31.3

# ── Evaluation (Phase 4) ────────────────────────────────────────────────────
# Was originally placeholder-pinned at 0.2.6 below (guessed before this phase
# was actually built); the real current release is 0.4.3, and its API has
# moved on substantially since 0.2.x — see src/run_eval.py's docstring for
# specifics (SingleTurnSample/EvaluationDataset instead of a plain HF
# Dataset with question/answer/contexts/ground_truth columns, several of the
# top-level imports this file uses are now deprecated in favor of a newer
# "collections" module, etc.). Pinned here so a future `pip install` doesn't
# silently jump to a version with yet another API shift.
ragas==0.4.3

# ── Chat UI (Phase 5) ────────────────────────────────────────────────────────
# Was placeholder-pinned at 1.41.1 below (guessed before this phase was built);
# the real current release is 1.64.0 — verified installed and working
# alongside the rest of this project's dependencies, not assumed.
streamlit==1.64.0

# ── Phase 5+ (uncomment when needed) ──────────────────────────────────────────
# langsmith==0.2.3
